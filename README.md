# OneSignal Remove Users

Bulk remove OneSignal subscribed users based on ONESIGNAL_PLAYER_ID from a csv export file.

## Usage
Place the csv export file and the php script on the same location.
```bash
$ php -f onesignal.php
```
